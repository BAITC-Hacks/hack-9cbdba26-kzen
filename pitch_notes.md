# Заметки к защите

Заполняется **по ходу работы**, а не в последний час. Всё, что здесь накопится,
превращается в слайды почти без переписывания.

## Кейс

- Чья боль и сколько стоит: менеджер отдела закупа ТОО «Электрокомплект» считает
  потребность вручную в Excel. Расчёт трудоёмкий, поэтому делается редко: по одним
  позициям склад затоварен, по другим дефицит. Разовые крупные продажи одному клиенту
  раздувают «регулярную» потребность.
- Что генерируем: рекомендованный заказ по каждому артикулу, сгруппированный по
  поставщикам, с обоснованием из компонентов расчёта и срочностью.
- Метрика организаторов: пять проверок Must have из п. 7 ТЗ (влияние всех источников,
  сезонность, компенсация stockout, устойчивость к разовым заказам, обоснование каждой строки).
- Формат сдачи: сервис + интерфейс; итог для менеджера — утверждённый заказ в xlsx
  с кодами 1С. Поставщику сервис ничего не отправляет (запрет п. 9 ТЗ).

## Находки в данных

Конкретно и с цифрами: «у абонентов с 3+ обращениями отток выше в 2.4 раза».

- Проверены 12 Excel-выгрузок IEK и Systeme Electric без изменения исходных файлов.
- Продажи в транзакциях положительные: IEK — 171 471 строка, Systeme — 76 998; отрицательные строки — возвраты (115 и 302).
- Обнаружены разные единицы IEK (`шт`, `м`, `упак`, `компл`), пропуски остатков, отрицательные месячные итоги и конфликты «артикул поставщика ↔ код 1С».
- **Колонка `Заказ` в рабочей Excel-модели менеджера (Systeme, лист `TDSheet`) пуста во всех 497 строках** — это ровно то, что заполняет наш сервис. Их текущая формула видна в той же таблице: среднее за 12 мес × коэффициент роста × коэффициент сезонности − свободный остаток − путь. Это наш baseline.
- **`Кэф. Роста` в модели менеджера — темп, а не множитель:** формула ячейки
  `SUM(AL:AN) / SUM(Z:AB) - 1` сравнивает последние три месяца с предыдущими.
  Значения лежат от −8,4 до 17,0, медиана −0,21; 289 из 497 отрицательные.
  Корректный множитель — `1 + коэффициент`, но значения ниже −1 всё равно меняют
  знак прогноза: это следствие возвратов/отрицательных агрегатов и требует
  согласованного с партнёром ограничения, а не слепого умножения.
- **У товара в пути ИЭК есть даты поступления** в заголовках колонок: «ПП УТ-7848 от 7 сентября 2026 г. (поступление до 01.10.2026)», 6 документов с ETA от 30.09 до 15.10.2026. Номера `УТ-…` — документы 1С:Управление торговлей.
- **У 6 позиций Systeme вместо кода 1С стоит артикул поставщика** (`ATN000330` и др.) — в 1С такую строку не загрузить. Мы не заказываем их молча, а помечаем «нужны данные».
- **ID клиента в данных нет**, хотя ТЗ его упоминает. «Крупная продажа одному клиенту» = одна накладная с аномальной долей; номер накладной — проверяемый proxy клиента.
- Склады есть только в модели Systeme: `Розничный склад` (268 SKU с остатком), `Витрина` (88), `Остаток ТЗ` (47), `РЦ ЕКТ Рыскулова` (0); в транзакциях везде `Алматы`.
- Категории Systeme (`Категория 2026`): 3 — 243 SKU, 1 — 124, 2 — 112, 5 — 11, 7 — 7.
- В файлах транзакций есть итоговая строка `Итого` и даты 2023 года в файле «2025–2026» — парсер обязан это учитывать.

## Что сделано

- Расчётное ядро принимает нормализованные `Sku` и параметры независимо от HTTP.
- Добавлено различие между подтверждённым stockout и отсутствующей историей остатка.
- Крупный клиентский заказ подтверждается номером накладной; LLM ничего не считает.
- Потребность учитывает спрос, сезонность, рост, свободный остаток, резерв, товар в пути и MOQ; каждое число попадает в структурированное объяснение.
- Категория Systeme влияет на страховой запас (1 → +10 п.п., 2 → +5 п.п.) — Must have 1 проверяется буквально.
- **Агент закупщика:** проверяет данные → считает → ищет аномалии → **сам добавляет проверки** по найденным флагам (разбор истории продаж, ограничения поставщика) → формирует версию на утверждение. Каждый шаг виден в ленте как бизнес-событие. Бюджет шагов ограничен.
- **Статус каждой позиции** вместо молчаливого нуля: «готово», «проверить» (разовые продажи > 30% истории, stockout > трети истории, заказ > 6 мес спроса, короткая история), «нужны данные» (нет кода 1С, нет истории). Позиция «нужны данные» не попадает в файл для 1С, пока менеджер не поставит количество сам.
- **Разделение ответственности:** флаги ставит код, какую проверку добавить — решает агент, количество считает воспроизводимая функция, утверждает человек. У агента нет инструмента, который пишет количество или отправляет заказ.
- **Рабочий процесс:** версии расчёта; ручная правка только с причиной; «рекомендовано» и «итог» хранятся рядом; менеджер отправляет на согласование, руководитель утверждает или возвращает; любая правка после согласования его снимает; утверждение привязано к ревизии, которую человек видел (иначе 409).
- **What-if «поставка задерживается»:** пересчёт позиции тем же расчётом → новая версия → нужно новое утверждение, старая остаётся как запись.
- **Файл для 1С:** xlsx, лист на поставщика, колонки из выгрузок партнёра (`Код 1с`, артикул поставщика, единица), обоснование и срочность в каждой строке (п. 5 ТЗ); состав колонок, разделитель и кодировка настраиваются.
- **Отправки поставщику нет** ни в каком виде — закреплено тестом (в API нет ручек send/submit поставщику).
- **Обоснование словами через LLM с проверкой чисел** (`GET /skus/{id}/narrative`): OpenAI (`gpt-4o-mini`, адаптер по протоколу OpenAI, провайдер меняется адресом в `.env`) формулирует текст из компонентов расчёта, сервис сверяет каждое число в ответе с расчётом; чужое число → текст отброшен, показан шаблон. Без ключа или сети — шаблон, демо не падает. В облако уходят код, название и компоненты расчёта; клиентов, накладных и цен там нет. Тот же адаптер с другим `base_url` работает с локальной моделью в контуре партнёра.
- **Агент разбирает первыми позиции к заказу с подтверждённой накладной** и наибольшей долей разовых продаж — на демо в карточке `IEK:081100392_` виден шаг агента «Разобрана история продаж».
- **Файл из интерфейса** (`POST /orders/export`) теперь содержит срочность, статус, обоснование и кто утвердил — как требует п. 5 ТЗ; раньше эти колонки были только в `/drafts/{v}/export`.
- Если данные не загрузились, сервис поднимается на демо-наборе и честно пишет в шапке `demo`.

## Утечки и подводные камни

Обучения нет, поэтому классической утечки таргета нет. Подводные камни другого рода:

- Транзакции покрывают не всю историю (по `200400085_`: помесячно 200 272 шт, в транзакциях 84 560) — спрос считаем по помесячным файлам, транзакции только для поиска разовых сделок.
- Исключение выбросов конфликтует с сезонностью: декабрьский пик похож на разовую сделку. Эталон берём по тому же месяцу других лет, с потолком в три медианы.
- Stockout не дан явно, выводится из нулевого остатка на начало месяца.
- Срок поставки в данных отсутствует — это параметр поставщика (по умолчанию Systeme 30 дн., ИЭК 35 дн.) и подписан в интерфейсе как предположение.

## Результаты

ML-метрики «baseline → финал» к нам не применимы: это расчёт, а не модель. Что показываем вместо них:

| Проверка | Результат | Где видно |
|---|---|---|
| Пять требований п. 7 ТЗ вживую | 5 из 5 | экран «Проверки ТЗ», `GET /api/v1/validation/scenarios` |
| Автотесты | 75, без внешних сервисов и файлов партнёра | `make test` |
| Формула менеджера против нашей на `081100392_` | 33,5 против 6,5 шт/мес; накладная №20000044499 на 400 шт | карточка SKU, демо-сценарий |
| Каждый источник меняет заказ | история, остаток, путь, категория и прирост проверяются параметризованным тестом | `test_each_source_changes_result` |

### Формула менеджера против очистки разовых заказов

Срез данных: Systeme Electric, расчёт на 23.09.2026. Формула из TDSheet
воспроизведена как `среднее 12 мес × (1 + рост) × (1 + сезонность) − свободный
остаток − путь`, затем результат округлён до MOQ. Наш вариант — `baseline` с
параметрами по умолчанию: срок 45 дней, покрытие 1 месяц, страховой запас 20%.

| Код 1С | Артикул | Разовая накладная | Доля транзакционной истории | Формула TDSheet | Наш заказ |
|---|---|---|---:|---:|---:|
| `300200441_` | `ATN000391` | №20000037722, 15.04.2026, 50 шт | 84,7% | 10 шт | 0 шт |
| `300200533_` | `ATN000944` | №20000107910, 15.09.2026, 23 шт | 79,3% | 0 шт | 0 шт |
| `300200896_` | `GAL000945` | №20000102119, 03.09.2026, 100 шт | 69,9% | 0 шт | 0 шт |
| `300200747_` | `ATN540174` | №20000102811, 04.09.2026, 20 шт | 60,6% | 6 шт | 6 шт |

Почему часть заказов совпадает с нулём: текущего свободного остатка достаточно.
Разовая накладная всё равно исключается из регулярного спроса, чтобы не раздуть
следующий заказ после расходования остатка.

Сильная находка для слайда: ячейки TDSheet показывают, что `Кэф. Роста` и
`Кэф. Сез-ти` — темпы изменения (`... - 1`), а не готовые множители. Рост лежит
в диапазоне от −8,4 до 17,0; 289 из 497 значений отрицательные. Прямое умножение
на коэффициент даёт отрицательный прогноз, а даже форма `(1 + коэффициент)` при
значениях ниже −1 меняет знак и требует бизнес-ограничений. Для `300200441_`:
среднее 4,83, рост −2,5, сезонность −3,5 — без ограничений два отрицательных
множителя превращаются в ложный положительный заказ 10 шт. Наш расчёт ограничивает
рост, очищает накладную на 50 шт и при свободном остатке 18 шт рекомендует 0.

### Реальный what-if по ETA

Документ `УТ-7848` из файла «Путь ИЭК» имеет исходную ETA 01.10.2026. Для SKU
`200400026_` (`ERO21-K01-16-DC`) в нём находится 720 шт. При параметрах демо
(дата расчёта 23.09.2026, срок ИЭК 35 дней, период пересмотра 14 дней,
страховой запас 20%, метод `smoothed`) рекомендация равна 396 шт. Если перенести
ETA этой партии на 20.11.2026, она перестаёт успевать к горизонту, и рекомендация
становится 1 116 шт. Всего перенос `УТ-7848` меняет заказ у 78 SKU ИЭК.

## Факторы влияния

Что двигает рекомендованное количество, на языке менеджера:

1. Регулярный спрос после исключения разовых сделок одному клиенту.
2. Месяцы без товара на складе — спрос восстанавливается, иначе заказ занижается и дефицит повторяется.
3. Сезонность месяца поставки и устойчивый рост.
4. Свободный остаток и товар в пути (с учётом того, успевает ли поставка).
5. Категория товара (страховой запас) и кратность отгрузки поставщика.

## Производительность

`python scripts/bench.py` на реальных данных партнёра (23.09, локально, один процесс):
**3909 SKU** (ИЭК 3185, Systeme Electric 724), `data_mode: imported`, предупреждений нет.

| Ручка | p50 | p95 |
|---|---|---|
| `POST /calculations` — агент считает весь ассортимент | 878 мс | 906 мс |
| `POST /recommendations/search` — таблица, страница 100 | 33 мс | 107 мс |
| … «что если» задержка 14 дн (пересчёт всех 3909 SKU) | 798 мс | 880 мс |
| `GET /skus/{id}/explanation` — карточка позиции | 3.6 мс | 4.7 мс |
| `GET /orders/current` | 8 мс | 9 мс |
| `POST /orders/export` — xlsx для 1С | 182 мс | 274 мс |

Фраза для слайда: **«весь ассортимент 3909 позиций пересчитывается меньше чем за секунду;
карточка позиции — 4 мс»**. Загрузка 12 Excel-выгрузок при старте — ~25 с, один раз.

Статусы позиций после расчёта:

| Статус | Позиций |
|---|---|
| к заказу (`order`) | 809 |
| хватает (`enough`) | 1993 |
| нужны данные (`needs_data`: нет кода 1С и т. п.) | 215 |
| мало истории (`insufficient_history`) | 892 |
| агент поставил «проверить» (`needs_review`) | 1368 |

Агент на прогоне: 16 шагов в ленте; флаги — разовые продажи 870 позиций, частый
stockout 701, крупный заказ 201; разовых отгрузок, подтверждённых номером накладной,
в позициях к заказу — 7.

_Честно про `needs_review`: 35% ассортимента — много для ручной проверки. Основная
причина — «товара не было больше трети истории». Кандидат на «что дальше»: сортировать
проверку по деньгам, а не показывать всё подряд._

## Демо

Три минуты, данные партнёра, роль переключается в шапке. Номера — из прогона 23.09.

1. **Запуск расчёта** (экран «Данные» → «Рассчитать»).
   Показываем: 3909 позиций, 809 к заказу, меньше секунды.
   _Жюри: «Менеджер делал это в Excel днями — здесь весь ассортимент двух поставщиков за секунду.»_
2. **Лента агента** (там же, trace).
   Показываем: проверка данных → расчёт → проверка аномалий (870 разовых, 701 stockout,
   201 крупный заказ) → агент сам добавляет разбор истории по подозрительным позициям.
   _Жюри: «Агент решает, что перепроверить; количество он не считает и не утверждает.»_
3. **Разовая продажа исключена** — `IEK:081100392_` «LED Лампа T8 18w 230v 4000K G13 IEK».
   В апреле 2026 накладная **20000044499 на 400 шт** при обычной 6 шт; остальные месяцы 0–2 шт.
   Среднее «как в Excel» за 12 мес — 33.5 шт/мес; у нас спрос 6.5 шт/мес → **заказ 5 шт**.
   _Жюри: «Одна накладная раздула бы заказ в десятки раз — мы показываем номер, менеджер проверит его в 1С.»_
4. **Нужны данные** — `SE:ATN000446` «Роз. с з/к со з/ш с крышкой 16А IP20» (Systeme).
   Вместо кода 1С стоит артикул поставщика — статус «нужны данные», количество «—», а не 0.
   _Жюри: «Где данных не хватает, мы не выдумываем ноль — говорим, чего нет, и в 1С это не уходит.»_
5. **Ручная правка с причиной** — `IEK:250600184_` «ЩРВ-П 18 модулей встр. пластик IP41».
   Рекомендовано 19 (накладная 20000085961 на 60 шт уже исключена) → ставим 24,
   причина «подтверждённый объект». Без причины сервис не даст сохранить.
   _Жюри: «Рекомендация и решение человека хранятся рядом — видно, кто и почему поменял.»_
6. **Согласование** — «Отправить на согласование» → роль «Руководитель» → «Утвердить».
   Если заказ поменяли после того, как руководитель открыл экран, — 409, утверждение не пройдёт.
   _Жюри: «Поставщику ничего не уходит само — это прямой запрет ТЗ, и он закреплён тестом.»_
7. **Файл для 1С** — «Скачать xlsx»: лист на поставщика, `Код 1с`, количество, срочность,
   обоснование, причина правки, кто утвердил.
   _Жюри: «Файл в формате их же выгрузок из 1С, ключ — код 1С.»_

Запасной ход, если спросят «а если поставка задержится»: «что если» +14 дней в таблице —
пересчёт 3909 позиций за 0.8 с, без сохранения в версию.

## Бизнес-эффект

Воспроизводимый прогон `python scripts/business_effect.py` по всем **3 909 SKU**,
дата расчёта 23.09.2026. Это контрфактический аудит одного снимка, не годовой
backtest и не обещание экономии.

| Риск текущей формулы | Затронуто | Изменение заказа | Денежная оценка |
|---|---:|---:|---:|
| Все разовые месячные выбросы без очистки | 207 позиций | +18 362 учётные единицы перезаказа: 14 241 шт, 4 040 м, 81 упак | **3 111 320 ₸** лишней себестоимости по 40 Systeme-позициям с `СС реал` |
| Подтверждённые номерами крупные накладные | найдено 56; текущий заказ меняется у 4 | +28 шт перезаказа | 14 931 ₸ по оценённой Systeme-позиции |
| Stockout без компенсации | 435 позиций | −13 168 единиц недозаказа: 7 170 шт, 5 830 м, 167 упак, 1 компл | упущенная маржа неизвестна — не выдумываем |

`baseline` и `smoothed` расходятся по 491 позиции: `smoothed` выше по 340,
`baseline` выше по 151; по 3 418 позиции результат одинаков. Номинальная сумма
заказа — 154 988 против 205 209 единиц, но смешанные `шт/м/упак/компл` нельзя
превращать в одну финансовую метрику. Без хронологического backtest не заявляем,
что `smoothed` точнее: **KEEP BASELINE**, а очистку выбросов и компенсацию stockout
оставляем как проверяемые обязательные правила.

Допущения, которые назовём вслух:

- 3,11 млн ₸ — себестоимость потенциального лишнего заказа в текущем снимке,
  а не годовая экономия; цена хранения, маржа и оборачиваемость партнёром не даны;
- количества разных единиц показаны раздельно, общий номинальный итог нужен только
  для технического сравнения методов;
- доля месяцев без товара по ассортименту: медиана 15%, у 39% позиций — больше трети года;
- время менеджера на ручной расчёт → минуты на проверку и утверждение.

## Что дальше

- LLM-планировщик агента поверх тех же инструментов (правила остаются запасным вариантом) + проверка, что каждое число в тексте LLM взято из расчёта.
- Импорт файлов из интерфейса с сопоставлением колонок.
- Загрузка файла прямо в документ «Заказ поставщику» 1С (формат согласовать с партнёром).
- Бэктест: заказ на начало 2026 по нашей формуле против факта — сколько дефицита избежали и сколько лишнего запаса не купили.

## Вопросы жюри и наши ответы

- **Вопрос партнёру:** как в действующей модели ограничиваются `Кэф. Роста` и
  `Кэф. Сез-ти`, если формула даёт значение ниже −1 или выше разумного диапазона:
  обрезаются вручную, заменяются экспертной оценкой или используются как `1 + k` без ограничения?

- **Почему не ML?** Это расчёт потребности: статистика, сезонность, правила поставщика. Обучать нечего, а каждое число должно быть объяснимо и воспроизводимо.
- **Откуда уверенность без обучения?** Пять проверок ТЗ проходят вживую; обоснование каждой строки собрано из компонентов расчёта — любую цифру менеджер пересчитает руками.
- **Что если данных не хватает?** Позиция получает статус «нужны данные» с причиной, а не заказ 0. В файл для 1С она не попадает без решения человека.
- **Где здесь AI и где агент?** Агент выбирает дополнительные проверки по найденным флагам и показывает шаги. Количество он не считает и не утверждает — это гарантия против выдуманных цифр.
- **Почему нет автоотправки поставщику?** Прямой запрет п. 9 ТЗ. Последний шаг — всегда утверждение человеком и файл для 1С.
- **Совместимо ли с их учётной системой?** Выгрузка в формате их выгрузок из 1С, ключ — `Код 1с`; точный формат загрузки согласуем с партнёром.
- **Что при росте нагрузки?** Расчёт по ~3000 SKU — доли секунды; версии сейчас в памяти одного процесса, для прода — таблица в Postgres.
