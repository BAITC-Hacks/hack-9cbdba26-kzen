# Расчёт заказов поставщикам

Сервис считает рекомендованные заказы для пополнения склада ТОО «Электрокомплект»:
история продаж и остатков → потребность по каждому артикулу → список заказов
по поставщикам с обоснованием каждой строки → проверка и утверждение менеджером
→ файл для загрузки в 1С. Заказ поставщику сервис не отправляет никогда.

Кейс, карта данных и ловушки в файлах партнёра — в `CASE.md`.
Статус задач команды — в `docs/CHECKLIST.md`.

## Что внутри

**Расчётное ядро, а не ML.** Статистика, сезонность, тренд, выбросы, кратность.
Обучения нет: всё считается за миллисекунды по 36 числам на артикул.

**Два метода прогноза, переключаются в запросе:**

| Метод | Что делает | Зачем |
|---|---|---|
| `baseline` | среднее за 12 мес × рост × сезонность | повторяет текущую формулу партнёра, точка отсчёта |
| `smoothed` | экспоненциальное сглаживание, отдельная ветка для редких продаж | реагирует на изменения спроса быстрее |

**Агент закупщика поверх расчёта.** Проверяет данные, считает, ищет аномалии
и по найденным флагам сам запускает дополнительные проверки. Количество считает
только расчётное ядро, утверждает только человек. Подробно — раздел «Агент и утверждение».

**Пять требований ТЗ как живые проверки.** `GET /api/v1/selfcheck` прогоняет
их на синтетических артикулах и показывает «до/после» по каждому.

## Для Claude Code

В корне лежит `CLAUDE.md` — контекст проекта: правила слоёв, команды, куда вносить
правки под кейс, тайминг и заморозка. В `.claude/skills/` пять скиллов, которые
подхватываются автоматически: разбор кейса, обучение, архитектура бэкенда,
скорость инференса, подготовка защиты. Отдельно настраивать ничего не нужно —
достаточно открыть папку проекта.

## Запуск за две минуты

```bash
cp .env.example .env
make setup        # зависимости
make api          # сервис на http://localhost:8000/docs
make test         # тесты
```

В Docker: `make up`, с кэшем — `make up PROFILES="--profile redis"`.

Проверить, что всё живо:

```bash
curl localhost:8000/health
curl localhost:8000/api/v1/selfcheck
curl -X POST localhost:8000/api/v1/orders/calculate \
     -H 'Content-Type: application/json' -d '{"lead_time_days": 45}'
```

## Архитектура

```
        HTTP
         │
    ┌────▼─────────────────────────────────────────┐
    │  api/         роутеры, схемы, ошибки          │
    ├──────────────────────────────────────────────┤
    │  application/ сценарии кейса (use cases)      │
    ├──────────────────────────────────────────────┤
    │  domain/      сущности, порты, правила        │  ← ничего не импортирует
    ├──────────────────────────────────────────────┤
    │  infrastructure/  CatBoost · DuckDB · Redis   │  → реализует порты
    └──────────────────────────────────────────────┘
         ▲
    core/container.py — единственное место, где выбираются реализации
```

Зависимости направлены внутрь: `api → application → domain`, а `infrastructure`
реализует интерфейсы домена. Практический смысл — не красота схемы, а то, что
модель, кэш и хранилище меняются строкой в `.env`, а тесты идут без Redis и Postgres.

### Как задать состав сервиса

Два равноправных способа: настройками или прямо в коде.

**Способ 1 — настройки (`.env`).** Удобно переключать на ходу, ничего не пересобирая.

| Настройка | Значения | Когда менять |
|---|---|---|
| `MODEL_BACKEND` | `dummy`, `artifact`, `llm` | `artifact` после `make train`; `llm` если обучать некогда |
| `CACHE_BACKEND` | `memory`, `redis` | `redis` только при нескольких воркерах |
| `STORAGE_BACKEND` | `memory`, `parquet`, `postgres` | `parquet` для датасета кейса |

**Способ 2 — сборка в коде.** Когда состав решён один раз и навсегда, явная сборка
честнее: её видно при чтении, она проверяется линтером и не ломается от опечатки в `.env`.
Готовые сборки лежат в `app/core/presets.py` — `dev` (заглушки), `demo` (артефакт +
parquet + кэш в памяти), `scaled` (то же с Redis), `no_training` (оценка через LLM). Переключение — одна строка в `main.py`:

```python
from app.core.presets import demo
app = create_app(container_factory=demo)
```

Своя сборка пишется там же и может комбинироваться с настройками — передавайте
только то, что хотите зафиксировать:

```python
async def my_setup(settings):
    return await build_container(
        settings,
        model=ArtifactModel(Path("ml/artifacts/model.joblib"),
                            Path("ml/artifacts/metadata.json")),
        storage=ParquetStorage(Path("data/dataset.parquet"), "subject_id"),
        # кэш не передан — возьмётся из настроек
    )
```

**Разница в поведении при сбое.** По настройкам сервис деградирует, но живёт: нет модели —
работает заглушка, нет Redis — кэш в памяти, нет датасета — пустое хранилище, а `/health`
отдаёт `degraded` с причинами. Упавшее на защите демо стоит дороже, чем демо без кэша.
Переданный же явно компонент не подменяется: если в коде написано «здесь артефакт»,
сервис упадёт при старте, а не сделает вид, что всё хорошо.

## Эндпоинты

| Метод | Путь | Зачем |
|---|---|---|
| POST | `/api/v1/orders/calculate` | заказы по поставщикам с обоснованием |
| GET | `/api/v1/orders/{code}` | карточка артикула: сколько заказать и почему |
| GET | `/api/v1/selfcheck` | пять проверок из пункта 7 ТЗ вживую |
| GET | `/api/v1/stats` | номенклатура, поставщики, доля месяцев без товара |
| POST | `/api/v1/agent/runs` | агент готовит закупку: статусы позиций + trace шагов → версия на утверждение |
| POST | `/api/v1/agent/runs/{v}/inbound-delay` | what-if «поставка задерживается» → пересчёт → новая версия |
| POST | `/api/v1/drafts` | расчёт без агента, сохранённый как версия |
| GET | `/api/v1/drafts`, `/api/v1/drafts/{v}` | список версий; версия с позициями, журналом и trace |
| PATCH | `/api/v1/drafts/{v}/lines/{code}` | ручная корректировка, причина обязательна |
| POST | `/api/v1/drafts/{v}/approve` | утверждение по ревизии с экрана; устаревшая ревизия → 409 |
| GET | `/api/v1/drafts/{v}/export` | `purchase_order_1c.xlsx` (или `?format=csv`), только после утверждения |
| GET | `/health` | `ok` или `degraded` с причинами |
| GET | `/metrics` | счётчики и p50/p95 латентности |

Параметры расчёта (`lead_time_days`, `coverage_months`, `safety_factor`, `method`)
передаются в запросе — менеджер может крутить их в интерфейсе и видеть,
как меняется заказ.

## Логика расчёта

```
история продаж по месяцам
  → срезать разовые крупные продажи        (Must have 4)
  → компенсировать месяцы без товара        (Must have 3)
  → прогноз: рост и сезонность              (Must have 2)
  → потребность на срок поставки + запас
  → минус свободный остаток и товар в пути  (Must have 1)
  → округлить до кратности отгрузки
  → срочность и обоснование                 (Must have 5)
```

Тонкость, которую стоит знать: **исключение выбросов конфликтует с сезонностью**.
Если сравнивать месяц со средним по всей истории, декабрьский пик выглядит как
разовая сделка и срезается — сезонность исчезает. Поэтому эталон берётся
по тому же календарному месяцу других лет, с потолком в три медианы на случай,
если само это наблюдение было выбросом. Разбор в `domain/demand.py`.

## Агент и утверждение

Сценарий менеджера:

```
выгрузки 1С → POST /agent/runs → проверка версии (статусы, trace, обоснования)
  → PATCH правки с причиной → POST approve → GET export → purchase_order_1c.xlsx → 1С
```

**Цикл агента** (`application/use_cases/agent.py`): очередь действий → вызов tool →
наблюдение в trace → политика решает, какие проверки добавить → повторять до пустой
очереди или конца бюджета шагов (`MAX_STEPS`).

| Tool | Что делает | Тип |
|---|---|---|
| `inspect_data_quality` | позиции, по которым считать нельзя | правила |
| `calculate_orders` | потребность по каждому артикулу + статус | расчётное ядро |
| `screen_recommendations` | флаги: всплеск, крупный заказ, частый stockout | правила |
| `review_one_off` | сколько исключено как разовые продажи, доля в истории | статистика |
| `check_supplier_constraints` | кратность, заказ в месяцах спроса | правила |
| `calculate_reorder_quantity` | пересчёт позиции в what-if | расчётное ядро |

**Кто что решает.** Флаги ставит код (`domain/assessment.py`): это факты о данных.
Политика решает, какую проверку добавить. Количество считает только `calculate_line`.
Утверждение и выгрузка — только человек, у агента таких tools нет. Сейчас политика
на правилах (`RulesPolicy`); LLM-политика встанет рядом с тем же интерфейсом,
правила останутся запасным вариантом.

**Статус позиции** — самое строгое из сработавших правил:

| Статус | Когда | Что происходит |
|---|---|---|
| `insufficient_data` | нет кода 1С, нет истории | не выгружается, пока менеджер не поставит количество с причиной |
| `needs_review` | история < 6 мес, stockout > 1/3, разовые продажи > 30% истории, заказ > 6 мес спроса | выгружается, но подсвечен на проверку |
| `ready` | ни одно правило не сработало | выгружается |

**Версии и утверждение** (`domain/workflow.py`):

- ручная правка остаётся в той же версии: растёт ревизия, утверждение снимается;
- изменились входные данные (задержка поставки) — создаётся **новая** версия со ссылкой
  на старую (`parent_version`), утверждение на неё не переносится;
- утверждение принимает ревизию, которую менеджер видел: если заказ успели изменить — 409;
- версии хранятся в памяти процесса: при перезапуске пропадают, на несколько воркеров
  не рассчитаны. Для прода — таблица в Postgres.

**Файл для 1С** (`infrastructure/export/onec_xlsx.py`): лист на поставщика, колонки
`Код 1с | Артикул поставщика | Номенклатура | Ед. | Рекомендовано | Количество | Склад |
Срочность | Статус | Причина корректировки | Обоснование | Версия расчёта | Утвердил`,
лист «Сводка». Обоснование и срочность — обязательный выход по п. 5 ТЗ. Колонки взяты из
выгрузок партнёра, ключ — `Код 1с`. Формат загрузки в конкретную версию 1С нужно
подтвердить у партнёра.

## Что ещё не сделано

- `infrastructure/storage/excel_loader.py` — заглушка: сервис поднимается на пустой
  номенклатуре. Ловушки, которые парсер обязан учесть, — в `CASE.md`.
- Товар в пути без дат поступления: what-if задержки вычитает партию из пути,
  а не сдвигает ETA. Даты есть в `Путь ИЭК` (заголовки «поступление до …»).
- LLM-политика агента и валидатор чисел в тексте LLM.
- Интерфейс.

## Тесты

```bash
make test
```

42 теста. Покрыто: очистка истории (выбросы, stockout, сезонность, тренд),
группировка по поставщикам, кратность отгрузки, влияние срока поставки,
переключение методов, ошибки 404 и 422, все пять требований пункта 7 ТЗ,
версии, правки с причиной, утверждение и его сброс, 409 на устаревшей ревизии,
гонка параллельных правок, три ветвления агента, what-if задержки, xlsx для 1С
и отсутствие ручек отправки поставщику.
Ни один тест не требует внешних сервисов и файлов партнёра.

## Скорость

| Операция | Время |
|---|---|
| расчёт по одному артикулу | 0.8 мс |
| самопроверка (5 требований ТЗ) | 2 мс |

Расчёт по всей номенклатуре (~3000 артикулов) укладывается в доли секунды,
поэтому никакой базы данных и фоновых задач не нужно.

## Решения и отказы

| Решение | Почему | От чего отказались |
|---|---|---|
| Расчёт вместо ML | задача решается статистикой; обучение не дало бы выигрыша за 5 часов | обученная модель, CatBoost, артефакты |
| Порты и адаптеры | реализации меняются строкой в `.env` или сборкой в коде, тесты без внешних сервисов | абстракции ради абстракций: интерфейс только там, где есть вторая реализация |
| Очистка истории в домене | нужна любому методу прогноза, а не конкретному | дублирование чистки в каждом прогнозисте |
| Номенклатура в памяти | 3000 артикулов × 36 месяцев считаются за доли секунды | база данных ради 15 МБ Excel |
| Кэш в памяти по умолчанию | при одном воркере быстрее Redis: нет сети и сериализации | Redis в обязательных зависимостях |
| `def` вместо `async def` на расчёте | CPU-задача в корутине заблокировала бы весь сервис | асинхронность ради асинхронности |
| Профили в compose | лишний сервис — лишняя точка отказа на демо | поднимать всё сразу |
| Деградация вместо падения | демо должно работать даже при отказе инфраструктуры | жёсткие проверки на старте |
| LLM отдельной ручкой | объяснение стоит сотни мс, оценка — единицы; связывать их значит потерять скорость | LLM внутри `/predict` |
| Агент: флаги кодом, количество функцией | число в заказе воспроизводимо, hallucination не доходит до 1С | LLM, который сам считает или решает |
| Статусы по правилам | «нет кода 1С» объяснимо, «confidence 0.73» — нет | числовой скор уверенности |
| Новая версия при смене фактов | утверждение относится к данным, на которых дано | правка утверждённой версии «на месте» |
| Файл для 1С, а не API | совместимость без доступа к их базе и за часы, а не недели | OData / COM-интеграция |
| Нет ручки отправки поставщику | запрет ТЗ; нет кода — нет риска | «отправить после утверждения» |

Эта таблица — готовый слайд. Умение объяснить, от чего отказались и почему,
ценится жюри выше, чем длинный список подключённых технологий.

## Структура

```
backend/app/
├── domain/          сущности, порты, demand.py — очистка истории,
│                    workflow.py — версии и утверждение, assessment.py — статусы позиций
├── application/     use cases: replenishment, drafts, agent
├── infrastructure/  forecasting/ storage/ cache/ llm/ export/ (xlsx для 1С)
├── api/             routes/ schemas/ deps errors middleware
├── core/            config logging container presets
└── main.py
ml/
├── src/config.py    параметры расчёта
└── notebooks/       шаблон разбора данных
tests/               расчёт, api заказов, версии и утверждение, агент
docs/CHECKLIST.md    статус задач команды
scripts/bench.py     замер латентности
CLAUDE.md            контекст для Claude Code
.claude/skills/      скиллы: кейс, обучение, архитектура, скорость, защита
pitch_notes.md       заметки к защите, заполняются по ходу работы
```
