# Чеклист команды — HackAlem AI, кейс «Электрокомплект»

Обновлено: 23.09.2026, ~15:20. Работаем **только в `main`**, веток нет.
Перед каждым push — `git pull --rebase`, коммиты маленькие.

Главный критерий — законченный сценарий:
**выгрузки 1С → анализ → решение → объяснение → утверждение менеджером → файл для 1С.**
Сначала закрываем 5 must-have, всё остальное потом.

Обозначения: ✅ готово · 🔄 в работе · ⬜ не начато · **P0** без этого нет демо · **P1** сильно усиливает · **P2** если останется время

---

## Человек 1 — Миша: данные, прогноз, расчёт заказа

Результат: модуль без HTTP. На входе нормализованные данные и параметры,
на выходе рекомендации со статусом и структурированным объяснением.
Контракт между модулем расчёта и API/агентом — раздел «Агент и утверждение» в `README.md`.

### Данные (P0 — блокер для всей команды)
- ⬜ **P0** `load_repository(data_dir)` в `excel_loader.py`: читает оба поставщика, возвращает `MemorySkuRepository`. Сейчас заглушка, сервис видит 0 артикулов
- ⬜ **P0** Добавить `pandas`, `openpyxl` в `.venv` и `openpyxl` в `backend/requirements.txt` (сейчас их нет)
- ⬜ **P0** Сопоставление `Код 1с` / `Номенклатура.Код` ↔ артикул поставщика
- ⬜ **P0** Товар в пути ИЭК **с ETA**: дата берётся из заголовка колонки «(поступление до 01.10.2026)»
- ⬜ **P1** Свободный остаток и резерв из модели Systeme (`Свободный остаток`, `Зарезервировано`)
- ⬜ **P1** Единицы измерения (шт / м / упак / компл), возвраты, строка `Итого`, дубли, коды не в формате 1С
- ⬜ **P1** Отчёт о качестве данных: цифры для экрана загрузки и для слайда

### Расчёт (5 must-have)
- ✅ Очистка stockout (`compensate_stockouts`) — есть в каркасе
- ✅ Разовые крупные продажи по месяцам (`remove_bulk_orders`) — есть в каркасе
- ✅ Сезонность и рост: `baseline` (формула менеджера) и `smoothed`
- ⬜ **P0** Товар в пути учитывается **только если ETA раньше горизонта защиты** (сейчас вычитается весь)
- ✅ Статус строки `READY` / `NEEDS_REVIEW` / `INSUFFICIENT_DATA` — **сделал Алдияр** в `domain/assessment.py` (нет кода 1С, история < 6 мес, stockout > 1/3, разовые > 30%, заказ > 6 мес спроса). Миша **дописывает правила туда же** (единицы, устаревший остаток, ETA), параллельный gate не нужен
- ⬜ **P1** Категория влияет на расчёт (например, категория → страховой запас), а не только на фильтр
- ⬜ **P1** Evidence по накладным для разовых заказов: номер, дата, количество, доля в истории
- ⬜ **P1** Проверка «аномально большой заказ» (больше 3 средних заказов или больше 6 месяцев запаса) → `NEEDS_REVIEW`
- ⬜ **P1** Проверка единиц: единица остатка ≠ единица MOQ → `INSUFFICIENT_DATA`
- ✅ MOQ и кратность, 0 → 1

### Проверки и цифры
- ✅ Самопроверка: 5 проверок из пункта 7 ТЗ (`/api/v1/selfcheck`)
- ⬜ **P1** Тест: сдвиг ETA за горизонт увеличивает заказ
- ✅ Тест: у SKU без кода 1С статус `INSUFFICIENT_DATA`, а не `quantity = 0` (`tests/test_agent_api.py`)
- ⬜ **P1** Сравнение «формула менеджера против нашей» на 4 SKU, где одна накладная больше 50% истории → в `pitch_notes.md`
- ⬜ **P2** Бэктест: заказ на начало 2026 → сколько stockout избежали и сколько лишнего запаса не купили
- ⬜ **P2** Документ методологии и ограничений (1 страница)

---

## Человек 2 — Алдияр: API, workflow закупщика, агент, интеграция с 1С

### Рабочий процесс (сделано)
- ✅ Версии расчёта: `POST /api/v1/drafts`
- ✅ Ручная корректировка с обязательной причиной: `PATCH /drafts/{v}/lines/{code}`
- ✅ Утверждение с проверкой ревизии (409 на устаревшем экране): `POST /drafts/{v}/approve`
- ✅ Сброс утверждения при правке утверждённого заказа
- ✅ Экспорт только утверждённого заказа: `GET /drafts/{v}/export` (xlsx по умолчанию, `?format=csv`)
- ✅ Автоотправки поставщику нет, закреплено тестом
- ✅ 18 API-тестов (drafts + agent), в том числе гонка потоков; всего в проекте 42
- ✅ Исправлен старт сервиса: не было `Settings.data_dir`

### Интеграция с 1С
- ✅ **P0** Экспорт `purchase_order_1c.xlsx`: лист на поставщика, колонки `Код 1с | Артикул поставщика | Номенклатура | Ед. | Рекомендовано | Количество | Склад | Причина корректировки | Версия расчёта | Утвердил`
- ⬜ **P1** Экспорт в формате модели менеджера: их `TDSheet` с заполненной колонкой `Заказ`
- ⬜ **P1** API загрузки выгрузок 1С (xlsx) + отчёт о качестве и дата актуальности
- ✅ **P1** Строки `INSUFFICIENT_DATA` не попадают в экспорт, пока менеджер не поставит количество вручную

### Агент (только поверх работающего расчёта на реальных данных)
- ✅ **P1** Схема trace-событий (бизнес-события, не chain-of-thought)
- ✅ **P1** `RulesPlanner`: фиксированный порядок tools + 4 ветвления (всплеск, нет данных, сдвиг ETA, большой заказ) — работает без LLM
- ✅ **P1** `POST /api/v1/agent/runs` → результат сохраняется версией в drafts
- ✅ **P1** What-if «поставка задерживается» → пересчёт → новая версия → новое утверждение (`POST /agent/runs/{v}/inbound-delay`; сейчас вычитает задержанное из пути, после `Inbound` с ETA у Миши — сдвиг даты)
- ⬜ **P2** LLM-планировщик (NVIDIA NIM): разбор свободного запроса → tool + параметры; не больше 8 шагов, whitelist tools
- ⬜ **P2** Валидатор чисел: каждое число в тексте LLM должно быть в outputs tools, иначе подставляется шаблон

### Интерфейс
- ⬜ **P0** Экран загрузки, ошибок и актуальности данных
- ⬜ **P0** Параметры (склад, категория, срок, запас) → запуск → таблица по поставщикам
- ⬜ **P0** Карточка товара: история, прогноз, формула по шагам, статус и причины
- ⬜ **P0** Правка количества с причиной → утверждение → скачивание xlsx
- ⬜ **P1** Поиск и фильтры (поставщик, статус, срочность)
- ⬜ **P1** Лента trace агента
- ⬜ **P2** Пересмотр исключённых продаж → повторный расчёт

### Сдача
- ⬜ **P0** Инструкция запуска (README), проверка полного сценария
- ⬜ **P0** Демо-сценарий и запись видео до 3:45
- ⬜ **P1** Латентность из `scripts/bench.py` → `pitch_notes.md`

---

## Демо (wow-момент)

1. **«Ваша формула против нашей»** на реальном SKU, где одна накладная больше 50% истории: показываем номер накладной, которая раздула заказ.
2. **Сдвиг ETA реального документа** «ПП УТ-7848, поступление до 01.10.2026» → агент пересчитывает (84 → 108), объясняет разницу, утверждение снимается.
3. **Approve → файл для 1С**: их же таблица с заполненной колонкой `Заказ`.
4. Одна фраза про gate: у 6 позиций Systeme нет кода 1С — не заказываем, говорим, чего не хватает.

## Не делаем даже при свободном времени

Прямая интеграция с 1С (OData/COM), multi-agent, чат «обо всём», LLM на пути расчёта,
база данных для версий, авторизация и роли, автоотправка поставщику, отдельный экран аналитики.

## Тайминг

| Время | Этап | Контрольная точка |
|---|---|---|
| до 2:00 | реальные данные загружаются | `/health` показывает skus > 0 |
| до 2:30 | сквозной сценарий на реальных данных | расчёт → правка → approve → xlsx |
| 2:30–3:45 | агент (RulesPlanner), what-if ETA, полировка демо | |
| 3:45 | **заморозка фич** | дальше только багфиксы и видео |
| 4:15–5:00 | слайды, два прогона питча | |

**Если к 2:00 реальные данные не загружаются** — агент откладываем, показываем workflow + gate + xlsx, агентность описываем на одном слайде.
