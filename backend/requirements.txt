# Зависимости образа. Отдельный файл, а не pyproject: он копируется в слой
# до исходников, поэтому пересборка после правки кода не переустанавливает пакеты.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
joblib>=1.4
catboost>=1.2
duckdb>=1.0
pyarrow>=16.0
openpyxl>=3.1
httpx>=0.27
redis>=5.0
